export TERM=seaos
export BASH_ENV=/etc/bashrc
export TERM=seaos
export PATH=/bin:/sbin:/usr/bin:/usr/sbin:/usr/libexec:/usr/i586-pc-seaos/bin:.

export USER="`id -un`"
if [ -n "${USER:+x}" ]; then
	export LOGNAME=$USER
	export HOME="/users/$LOGNAME"
fi

. /etc/bashrc
